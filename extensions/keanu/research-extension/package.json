{
  "name": "keanu-research-extension",
  "version": "0.1.0",
  "description": "Dialectical deep research that saves babies instead of throwing them out",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit",
    "watch": "node build.mjs --watch"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.52.0",
    "@mozilla/readability": "^0.5.0",
    "linkedom": "^0.18.0"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "esbuild": "^0.25.0",
    "typescript": "^5.7.0"
  }
}

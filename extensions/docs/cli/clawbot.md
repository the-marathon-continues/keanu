---
summary: "CLI reference for `keanu clawbot` (legacy alias namespace)"
read_when:
  - You maintain older scripts using `keanu clawbot ...`
  - You need migration guidance to current commands
title: "clawbot"
---

# `keanu clawbot`

Legacy alias namespace kept for backwards compatibility.

Current supported alias:

- `keanu clawbot qr` (same behavior as [`keanu qr`](/cli/qr))

## Migration

Prefer modern top-level commands directly:

- `keanu clawbot qr` -> `keanu qr`

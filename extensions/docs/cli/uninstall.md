---
summary: "CLI reference for `keanu uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `keanu uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
keanu uninstall
keanu uninstall --all --yes
keanu uninstall --dry-run
```
